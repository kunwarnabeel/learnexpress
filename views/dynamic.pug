doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Dynamic Pug
    body
    if(user)
    h2#id.id=user.id 
    h1#name.name=user.name
    a(href=user.url) URL
    <BR/>
    input(type="email" name="email" value=user.email)
    else
    a(href = "#") Sign Up
    //- else
    //- a(href = "/sign_up") Sign Up